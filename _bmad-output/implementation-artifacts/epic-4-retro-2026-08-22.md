---
epic: 4
date: 2026-08-22
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 4 Retrospective: Tech Lead Artifacts

## Epic summary

Epic 4 ("Tech Lead Artifacts") produced four documentation-only artifacts, no application code: `docs/adr/` (6 ADRs + README, Story 4.1), `docs/design/scaling-product-list-pagination.md` (Story 4.2), `docs/review/code-review-checklist.md` (Story 4.3), `docs/onboarding/mentoring-note.md` (Story 4.4).

**Diff range:** `910ae34..91a34d2` (parent of Story 4.1's commit through Story 4.4's commit).

**Stories completed, all at `review` status (not yet `done`):** 4-1-architecture-decision-records, 4-2-design-doc-scaling-path-for-a-non-trivial-feature, 4-3-code-review-checklist-applied-to-real-commits, 4-4-mentoring-onboarding-note. User confirmed proceeding with the retro over these unfinished statuses (same situation, same choice, as Epic 3's retro) — see Assumptions-equivalent note: this was an interactive confirmation, not a headless assumption.

**Evidence inventory:**
- Epic spec: `_bmad-output/planning-artifacts/epics.md` §"Epic 4: Tech Lead Artifacts" (lines 397-467) — declared acceptance criteria present for all 4 stories.
- Story specs: `spec-4-1-*.md` through `spec-4-4-*.md`, all `route: one-shot`, all `status: done` in their own frontmatter (the spec's own terminal status, distinct from sprint-status.yaml's `review`).
- Sprint status: `sprint-status.yaml`, current as of this run.
- Previous retrospective: `epic-3-retro-2026-08-20.md` exists; its 11 action items are all still `status: open` in `sprint-status.yaml`.
- Session logs: this conversation itself — full record of all four stories' implementation, blind-hunter reviews, and patches. Available and used.
- `git_evidence.py` commit-to-story attribution returned empty for all 4 commits (subjects use prose "Implement story 4.1: ..." not the exact slug format the script string-matches on) — same known limitation as Epic 3's retro. Not treated as blocking: commits were manually attributed from direct authorship knowledge (I wrote and committed all four this session) — `bed397f`→4.1, `27fd863`→4.2, `71c09f9`→4.3, `91a34d2`→4.4.

## Findings

**Aggregate views (inline, docs-only epic — no dependency graph, no god-class growth applicable):**

- **Spec-to-implementation reconciliation — clean.** All 4 stories' declared acceptance criteria (`epics.md` §Epic 4) checked against delivered artifacts: 4.1's ADR count (6, within 4-6) and honest-tradeoff requirement, 4.2's concrete-tradeoffs requirement, 4.3's 15-item count and real-finding requirement, 4.4's SOLID-explanation and DI-bug-first requirement all met. No silently dropped or added scope found.
- **Duplication map — intentional, verified consistent.** The Story 1.5 DI bug and the `CategoryService.DeleteAsync` check-then-act race are each deliberately described in three sibling documents (ADR + checklist + mentoring note). This is by design, not accidental duplication — but it creates a real cross-document consistency risk no single story's own review could see, which is exactly what this retro's diff-scope review (below) targeted.
- **Pattern divergence — none found.** All 4 documents follow the same `Status/Date/Author-or-Deciders` header convention and markdown-link cross-referencing style (one exception noted below, patched).

**Diff-scope review (`bmad-review`, adversarial + edge-case-hunter lenses; verification-gap screened out as non-behavioral per its own Step 1 — confirmed zero application code changed):**

15 unique findings surfaced (16 raised, 1 duplicate merged — the README date-range issue was independently caught by both lenses). All 15 fixed, deferred, or routed as an action item; **zero rejected as noise**:

- *Fixed in place (12):* `docs/adr/005-*.md` and `docs/adr/003-*.md` were missing their `AD-10`/`AD-5` self-identification (every other ADR names its `AD-#`); `docs/adr/README.md` overclaimed its decision window ("Epics 1-3, through 2026-08-20") when every ADR actually dates to Epics 1-2 through 2026-08-19, and didn't flag the already-logged missing-7th-ADR (CSRF) gap; the checklist's intro claimed Item 3 was "demonstrated against a real commit" while Item 3's own text says the opposite (no commit exists, it predates git init) — reworded to stop contradicting itself; the checklist's Item 5 never linked ADR-005 despite being its twin bug from a sibling story, and Item 3 never mentioned the `ValidateScopes`/`ValidateOnBuild` startup guardrail that ADR-006 treats as central to the fix; the checklist's Item 3 stated a forward-looking Epic 5 SBI-example proposal with the same factual confidence as its verified claims — reworded as explicitly speculative; the mentoring note's Item 2 was under-cross-referenced relative to Item 1 (asymmetric linking to the checklist) and attributed to ADR-006 a "no regression test" claim that ADR-006 itself only relays from `deferred-work.md`; all three retellings of the Category-delete race (ADR-005, checklist Item 5, mentoring note Item 2) dropped a real detail present in the source they trace to — the 500 is shaped as ProblemDetails, not a raw crash, since review added `UseExceptionHandler()`; the design doc cited `AD-2`/`AD-9`/`deferred-work.md` as bare text everywhere else in the doc set uses markdown links — converted (one exception left as bare text intentionally: `AD-8` inside a C# code *comment*, where a markdown link would misrepresent what a real code comment looks like).
- *Deferred (1):* `story-1-5-di-bug-log-excerpt.md`'s "line 42" fix citation has drifted (actual registration is now line 85) — pre-existing since Story 1.5, not caused by Epic 4; logged to `deferred-work.md` rather than edited, since the log excerpt is a historical record that was accurate when written.
- *Routed as action items (2):* ADR-006 lists `IPasswordHasher<User>` as a sanctioned DI Singleton exception; `ARCHITECTURE-SPINE.md`'s AD-4 rule text only names `ILogger`/`IConfiguration` — ADR-006 is the more accurate source (verified against `Program.cs:96`), so the spine's rule text should be updated to match, not the other way around. Neither of the two "twin bugs" this epic wrote up three times each (DI lifetime, check-then-act race) has an assigned owner anywhere in the 15 documents that discuss them — six independent "no tracked ticket" callouts, zero tickets.

## Behavior verification

Not applicable — Epic 4 changed zero application code (confirmed via `git_evidence.py`'s file list: every changed path is under `docs/` or `_bmad-output/implementation-artifacts/`). No runtime behavior exists to exercise.

## Previous-retro follow-through

Epic 3's retro (`epic-3-retro-2026-08-20.md`) logged 11 action items, all still `status: open` in `sprint-status.yaml`. None were addressed during Epic 4 — Epic 4's diff touches no `src/` or `client/` files, so none of the ProductForm/ProductList/AuthContext-focused action items could have landed as a side effect. No evidence any item landed; no status transition proposed.

## Action items

1. **Spec reconciliation:** Update `ARCHITECTURE-SPINE.md`'s AD-4 rule text to add `IPasswordHasher<User>` to the sanctioned Singleton-exception list, matching what `docs/adr/006-scoped-di-lifetimes.md` and the actual `Program.cs:96` registration already show. Owner: Prasadmallavalli.
2. **Backlog / ownership gap:** Neither the check-then-act race (Category delete, Product create/update, User registration) nor the missing DI-lifetime regression test has an owning story anywhere, despite six independent "no tracked ticket" callouts across Epic 4's own documentation. Create a follow-up story (unscheduled backlog) to build the Domain-level exception type fix and the DI regression test, rather than letting the documentation's honesty about the gap substitute for actually tracking it. Owner: Prasadmallavalli.
3. **Process lesson (upstream, recurring — also true of Epic 3's retro):** each story's own one-shot review checks only that story's own document in isolation; nothing checks a multi-story epic's documents against each other until the retro runs. This retro's diff-scope review caught 15 real cross-document issues (asymmetric cross-links, dropped details, a self-contradiction, stale metadata) that no single story's blind-hunter review could have seen, because none of them existed until sibling stories landed. Worth naming explicitly since it's the second epic in a row where the retro — not any story's own review — is what catches this class of defect. No process change proposed here beyond naming it; the retrospective step is already doing the job it's meant to do.

## Acceptance verdict

**Criteria:** declared (`epics.md` §Epic 4, all 4 stories). All declared acceptance criteria were checked against the delivered artifacts and met — see Findings § Spec-to-implementation reconciliation. No blocking finding remains open: of the 15 cross-document findings, 12 were fixed in place during this retro, 1 is a pre-existing (not Epic-4-caused) gap now logged to `deferred-work.md`, and 2 are forward-looking action items, not defects in what was delivered.

**Machine verdict: rejected** — `pending_stories` was non-empty (all 4 of 4-1 through 4-4 are `review`, not `done`) at detection time, which is a hard rule regardless of content quality (see `references/acceptance-verdict.md`, "Unfinished stories").

**Human decision:** the user overrode the machine verdict — all 4 stories marked `done` in `sprint-status.yaml`, final verdict **accepted-with-open-items** (2 tracked action items remain open, not because anything is broken, but because a spec reconciliation and a backlog story are proposed, not yet executed). Same resolution pattern as Epic 3's retro faced the identical tension.

## Open questions

None the findings above leave unresolved — every finding was either fixed, deferred with a stated reason, or routed to a named action item. The one open decision is the acceptance verdict override itself (above), which is the user's to make, not a gap in the analysis.

## Assumptions

Not a headless run — this section stays empty per the skill's convention (facts recorded in Epic summary instead where the user confirmed them interactively).

---
epic: 5
date: 2026-08-22
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 5 Retrospective: Engineering Manager Artifacts

## Epic summary

Epic 5 ("Engineering Manager Artifacts") produced five documentation-only artifacts, no application code: `docs/eng-mgmt/postmortem-di-captive-dependency.md` (Story 5.1), `docs/eng-mgmt/post-mvp-roadmap.md` (Story 5.2), `docs/eng-mgmt/estimation-calibration-note.md` (Story 5.3), `docs/eng-mgmt/hiring-question-set.md` (Story 5.4), `docs/eng-mgmt/sbi-feedback-example.md` (Story 5.5).

**Diff range:** `1a4c378..7e64d14` (parent of Story 5.1's commit through Story 5.5's commit).

**Stories completed, all at `review` status (not yet `done`):** 5-1-incident-postmortem-blameless, 5-2-post-mvp-roadmap, 5-3-estimation-calibration-note, 5-4-hiring-question-set, 5-5-feedback-framework-example-sbi. User confirmed proceeding with the retro over these unfinished statuses (same situation, same choice, as Epics 3 and 4's retros) — interactive confirmation, not a headless assumption.

**Evidence inventory:**
- Epic spec: `_bmad-output/planning-artifacts/epics.md` §"Epic 5: Engineering Manager Artifacts" — declared acceptance criteria present for all 5 stories.
- Story specs: `spec-5-1-*.md` through `spec-5-5-*.md`, all `route: one-shot`, all `status: done` in their own frontmatter.
- Sprint status: `sprint-status.yaml`, current as of this run.
- Previous retrospective: `epic-4-retro-2026-08-22.md` exists; its 2 action items (spec reconciliation of `ARCHITECTURE-SPINE.md`'s AD-4 text; a follow-up story for the check-then-act races/DI regression test) are both still `status: open` — confirmed neither landed during Epic 5 (`ARCHITECTURE-SPINE.md` untouched since the initial commit; no new story created).
- Session logs: this conversation itself — full record of all five stories' implementation, blind-hunter reviews, and patches. Available and used.
- `git_evidence.py` commit-to-story attribution returned empty for all 5 commits (same known limitation as Epics 3/4's retros — commit subjects use prose, not the exact slug format). Not treated as blocking: commits manually attributed from direct authorship knowledge — `b9e09d2`→5.1, `bbca11f`→5.2, `e9feac0`→5.3, `7b7751a`→5.4, `7e64d14`→5.5.

## Findings

**Aggregate views (inline, docs-only epic — no dependency graph, no god-class growth applicable):**

- **Spec-to-implementation reconciliation — clean.** All 5 stories' declared acceptance criteria (`epics.md` §Epic 5) checked against delivered artifacts: 5.1's timeline/impact/root-cause/fix/follow-up shape and Item-3 trace, 5.2's user/business-facing-reason-per-horizon requirement, 5.3's named-divergent-session requirement, 5.4's 5-8-question-count and Epic-1-4-grounding requirement, 5.5's real-finding-grounding requirement — all met. No silently dropped or added scope found.
- **Duplication map — intentional, but under-cross-referenced.** The Story 1.5 DI bug (checklist Item 3) is deliberately reused across four sibling documents (the checklist itself, the postmortem, the hiring question set's Q5, the SBI example) — by design, and a real strength (richest evidence trail in the epic). But before this retro, none of the four fully acknowledged the other three; each independently claimed to be one of only "two" or didn't cite the shared checklist anchor at all. Fixed during this retro (see Diff-scope review below).
- **Pattern divergence — minor, now closed.** Only the SBI document had a "Satisfies: Story X" header field; the other four didn't. Added to all five during this retro for consistency.

**Diff-scope review (`bmad-review`, adversarial + edge-case-hunter lenses; verification-gap screened out as non-behavioral per its own Step 1 — confirmed zero application code changed):**

21 findings raised across both lenses (13 adversarial, 8 edge-case), 2 pairs converged on the same underlying issue (the Q7→mentoring-note broken/missing link; the checklist-citation-overclaim), leaving 19 unique findings. All 19 fixed; **zero rejected as noise, zero deferred**:

- *Citation accuracy fixes:* the postmortem's Follow-up action 2 claimed checklist Item 3 "independently flags" the missing-regression-test gap — verified false (the checklist never mentions "regression test" anywhere; the real sources are `deferred-work.md` and ADR-006's Consequences) and corrected. Two broken relative links fixed (the SBI doc's checklist link was missing `../review/`, the same class of mistake Story 5.4's own review had already caught and fixed elsewhere in the epic — a cross-document consistency gap no single story's review could see). The hiring question set's Q7 gained a link to the mentoring note it was citing without one; Q2 gained an acknowledgment that the check-then-act race has three still-open instances, not the one it named; Q5 gained the checklist's Item 3 citation it was missing (present in the postmortem and SBI doc, absent here).
- *A real apparent contradiction, clarified:* the roadmap's Horizon 2 said the JWT signing key "sits in plaintext... with no documented plan," while the hiring set's Q8 described the same key as "rotated and scrubbed from git history" — resolved by distinguishing two real, non-contradictory facts the two documents had conflated: the specific leaked-value incident is closed (rotated, history-scrubbed), but the absence of an actual secrets-management *system* for whatever key exists now is a separate, still-open gap. Both documents now state this distinction explicitly.
- *Missing cross-references restored:* the roadmap's Horizon 3 gained the pre-commit-hook/secret-scanner gap the hiring set's Q8 had already surfaced but the roadmap's "known open gaps" inventory omitted; the roadmap gained a cross-reference to the estimation note's "budget review cycles, not hours" conclusion (explaining why none of the roadmap's Definitions of done name a calendar target — an intentional choice per this project's own no-time-estimates evidence, not an oversight); the checklist's stale "Epic 5 doesn't exist yet" forward-reference (written in Story 4.3, before Epic 5 existed) was updated to confirm the SBI example it predicted now exists.
- *The estimation note's own claims, invalidated by later stories in the same epic:* Story 5.3 was written and reviewed before Stories 5.4/5.5 existed, so its "Sessions 16-18 not yet built" claim went stale the moment Session 16 (hiring questions + SBI example) shipped 45-70 minutes later. Updated with a 5th real example (Session 16's split, and the sharp 11-vs-16 finding-count divergence between 5.4 and 5.5), a corrected table (3 new rows, corrected total), and a corrected Takeaway — the original "10-15 per artifact, consistently" claim overstated stability once Story 5.5's actual 16 and Story 5.3's own unreported 12 are both counted; the actual spread across all 5 stories is 11-16, not a constant, though still a more explicable unit than an hour estimate.

## Behavior verification

Not applicable — Epic 5 changed zero application code (confirmed via `git_evidence.py`'s file list: every changed path is under `docs/` or `_bmad-output/implementation-artifacts/`). No runtime behavior exists to exercise.

## Previous-retro follow-through

Epic 4's retro (`epic-4-retro-2026-08-22.md`) logged 2 action items, both still `status: open`. Neither landed during Epic 5: `ARCHITECTURE-SPINE.md` has no commits since the project's initial commit (confirmed via `git log`), and no new story exists for the check-then-act-race/DI-regression-test follow-up. No status transition proposed.

## Action items

No new action items — every one of the 19 findings from this retro's diff-scope review was fixed directly in the relevant document during Phase 2, rather than deferred or routed for later. Two carry-over items remain from Epic 4's retro (unaffected by Epic 5, see Previous-retro follow-through above):

1. **Carried over, still open:** Update `ARCHITECTURE-SPINE.md`'s AD-4 rule text to add `IPasswordHasher<User>` to the sanctioned Singleton-exception list. Owner: Prasadmallavalli. (`epic-4-retro-item-12`)
2. **Carried over, still open:** Create a follow-up story for the Domain-level exception type fix (check-then-act races) and the missing DI-lifetime regression test. Owner: Prasadmallavalli. (`epic-4-retro-item-13`)
3. **Process lesson (recurring, third epic in a row):** Epic 3's retro caught cross-story defects no single story's review saw; Epic 4's retro caught 15 cross-document issues the same way; this retro caught 19, including a citation that was factually wrong (the postmortem's checklist-corroboration claim) and a document whose own claims went stale mid-epic (the estimation note, invalidated by Stories 5.4/5.5 shipping after it). This is now a validated, repeating pattern, not a one-off: whenever an epic's stories deliberately cross-reference the same shared finding (as Epic 4's and Epic 5's docs both did, extensively, by design), no story's own single-document review can catch drift between siblings — only a pass that reads the whole epic's output together can. No process change is proposed here beyond naming this explicitly for a third time; the retrospective step is doing exactly the job it exists to do.

## Acceptance verdict

**Criteria:** declared (`epics.md` §Epic 5, all 5 stories). All declared acceptance criteria were checked against the delivered artifacts and met — see Findings § Spec-to-implementation reconciliation. No blocking finding remains open: of the 19 cross-document findings from this retro's review, all 19 were fixed in place; none required deferral.

**Machine verdict: rejected** — `pending_stories` was non-empty (all 5 of 5-1 through 5-5 are `review`, not `done`) at detection time, which is a hard rule regardless of content quality (see `references/acceptance-verdict.md`, "Unfinished stories").

**Human decision:** the user overrode the machine verdict — all 5 stories marked `done` in `sprint-status.yaml`, final verdict **accepted-with-open-items** (the 2 carried-over Epic 4 action items remain open, not because Epic 5's work is broken). Same resolution pattern as Epics 3 and 4's retros faced the identical tension.

## Open questions

None the findings above leave unresolved — every finding was fixed directly, with a source reference. The one open decision is the acceptance verdict override itself (above), which is the user's to make, not a gap in the analysis.

## Assumptions

Not a headless run — this section stays empty per the skill's convention (facts recorded in Epic summary instead where the user confirmed them interactively).
